---
name: backstage-writer
description: Write, rewrite, edit, or review Ticketmaster and Live Nation marketing copy in the approved brand voice — for fans, for clients and partners, or for an audience not stated. Use for any request to produce, improve or check copy, and for questions about tone, voice, copy style, whether something sounds on-brand, or do/don't language. Also for Fan Support writing — help articles, agent responses, apology language, error messaging; for formatting conventions — capitalization, punctuation, numerals, dates and times, genre; for copy tied to a named team, product, market or messaging pillar; for fan-facing trust and authenticity language; and for a quick informal check of a pasted message ("does this read OK?") with no audience or output type named. Which teams, products, markets and pillars are covered, and what happens when nothing governs a request, are determined by the live Confluence page this skill fetches, not this description. Do NOT use for performance reporting or narrative built from data.
---

# Backstage Writer

Before producing anything, fetch the live Backstage Writer page (`654651141`, space `GB2CM`) via the Confluence MCP connector and follow it exactly.

That page is the source of truth for this skill — what it fires on, which pages to read and when, how to write, the hard limits, what the output looks like, and the closing line. Do not reconstruct any of it from memory, and do not treat the `description` above as a substitute for reading the page.

## How pages are read

Only the most current version of any page is ever referenced. Read pages live. Whatever a page says at that moment is what applies, and where something has been taken off a page it no longer applies.

Nothing about the source pages is cached in this file. Read them.

## If a page can't be read

- **Can't be reached** — stop, say which page, and ask whether to wait or proceed with the gap flagged.
- **Reached but empty, or doesn't cover what's needed** — say so plainly and don't fill the gap. Carry on with the rest if the rest is enough.
- **Gone, deleted, or archived** — say so. This needs the skill's owner to repoint it; waiting won't fix it.

Never fall back on general knowledge for brand, legal, or compliance content.

## Escalation

Owners of source pages set the rules for their subject. They are the escalation point when a source is unclear, contradicts another page, or doesn't cover the request. **They do not approve individual outputs.**

Every output is a draft requiring human review before use — by the person who asked for the copy, or their team.

## Before editing the description

The `description` is the only part of this skill that can't be fetched live, so it's the only part that can go stale. It's written to describe the kinds of request the page covers, never the page's contents — no product names, no team names, no coverage gaps, no claims about other skills.

**A change to the Confluence page should not require a change here.** Regenerate only when the kind of work changes: a new mode, a new audience class, a new verb. The full rule is in Section 1 of page 654651141.
